BUG - changes on one device aren't reflected on the other until state it reset
pass api gateway url from serverless to react
use esbuild instead of webpack so serverless uses less memory
remove .serverless from git

backend:
    add authenticattion to addMeter and getMeters endpoint
    Add email authentication to signup

frontend:
    Before adding a meter make an API call to check the details can be fetched
    If API can not be called then an error should be displayed
    Add user settings to nav bar
    Changes filtering so that edge periods aren't cut off
    Move cloudfront distribution to IaC
    Make consumption page mobile friendly
    Show current carbon intensity for electricity
    Get carbon intensities for electricity and display CO2 emissions
    Show loading spinner whilst data is collecting from octopus API